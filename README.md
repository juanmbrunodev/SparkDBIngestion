# Spark Databases Ingestion Example Project

Project demonstrates usage of files loading functionality, for RDBMS such as PostgresSQL, within the *Spark* API.

To run execute:

`mvn install exec:exec`
